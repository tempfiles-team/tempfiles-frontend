import styled from '@emotion/styled';

export const ApiBox = styled.div`
  background-color: var(--color-backgorund-filelistbox);
  color: var(--color-text-tertiary);
  border-radius: 1rem;
  padding: 2rem 1.4rem 2rem 1.4rem;
  margin: 1rem;
  display: flex;
  flex-direction: row;
  align-items: center;
  font-size: 2.2rem;
  font-weight: 700;
  cursor: pointer;
`;

export const ApiDescText = styled.div`
  font-size: 2.6rem;
  font-weight: 700;
  margin: 1.5rem 1rem 2rem 1rem;
`;

export const ApiCommandBox = styled.div`
  background: var(--color-background-apicommand);
  font-size: 2.5rem;
  font-weight: 700;
  border-radius: 1rem;
  margin: 1rem 1rem 3rem 1rem;
  padding: 4rem 2rem 4rem 2rem;
`;
