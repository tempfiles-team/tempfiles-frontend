import styled from '@emotion/styled';

export const TextDeletePageContainer = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  margin-top: 9rem;
`;

export const TextDeletePageButtonSection = styled.div``;

export const TextDeletePagePageLabel = styled.div`
  font-size: 3rem;
  font-weight: 700;
  color: var(--color-text-primary);
  margin: 3rem;
`;
